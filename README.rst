==============
 Bookweald-rs
==============

Installation
------------

Install dependencies
....................

Ensure the ``libxml2`` library is installed::

  sudo apt install pkg-config libclang-dev libxml2-dev
